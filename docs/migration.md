# Migration

When adopting the template in an existing server, move capability registration into `buildServer`, preserve public tool and resource identifiers, then replace transport wiring. Run protocol integration tests before changing schemas or defaults.
