# Client examples

Local hosts should launch `dist/cli.js` over stdio. Remote clients connect to the configured Streamable HTTP endpoint.

The files under `examples/` demonstrate both paths. Replace absolute paths, ports, and server names for your environment. Keep credentials outside committed configuration.
