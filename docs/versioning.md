# Versioning

The project follows Semantic Versioning. Tool names, resource URIs, prompt names, schemas, and transport defaults are public interfaces.

Adding an optional capability is normally minor. Removing a capability, tightening accepted input, or changing a result shape requires a major version unless the previous behavior was demonstrably defective.
