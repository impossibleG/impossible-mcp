# Observability

Structured logs record lifecycle events and tool metadata without logging tool input. Extend the logger with request correlation and metrics appropriate to the deployment.

Keep protocol notifications separate from process logs. In stdio mode, stdout remains reserved for MCP frames.
