# Default limits

The template limits configuration values and example tool inputs, while leaving deployment-specific concurrency and payload policies to the application. Add explicit limits before introducing file uploads, model inference, database scans, or unbounded remote requests.
