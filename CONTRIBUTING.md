# Contributing

Use Node 22 or newer, install with `npm ci`, and run `npm run verify` before opening a pull request.

Keep capability handlers small, validate all external input, and avoid writing logs to stdout when using stdio. New behavior should include tests and documentation.
